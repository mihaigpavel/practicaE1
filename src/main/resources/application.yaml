spring:
  jpa:
    open-in-view: false
  application:
    name: e1
  datasource:
    url: jdbc:postgresql://localhost:5432/e1
    username: postgres
    password: mihai
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
    org.hibernate.orm.jdbc.bind: trace
    org.springframework.jdbc.core: trace
app:
  api-key: "1234"

